apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-mygha-svc
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ .Release.Name }}-mygha
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}